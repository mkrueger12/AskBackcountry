[
  {
    "name": "snow_depth_data",
    "description": "Returns Snow Depth and Snow Fall data. Can also return metadata on SNOTEL stations.",
    "parameters": {
      "type": "object",
      "properties": {
        "user_question": {
          "type": "string",
          "description": "How much new snow has fallen in the last 24 hours in Eagle County?"
        }
      },
      "required": [
        "user_question"
      ]
    }
  },
  {
    "name": "co_field_obv",
    "description": "Collects the past 7 days of Colorado Avalanche Center field reports",
    "parameters": {
      "type": "object",
      "properties": {
        "zone": {
          "type": "string",
          "description": "One of the follwoing: 'Steamboat & Flat Tops', 'Front Range', 'Vail & Summit County', 'Sawatch', 'Aspen', 'Gunnison', 'Grand Mesa', 'North San Juan', 'South San Juan', 'Sangre de Cristo'"
        }
      },
      "required": [
      ]
    }
  },
  {
    "name": "weather_forecast",
    "description": "Returns the 3-day weather forecast for a given location.",
    "parameters": {
      "type": "object",
      "properties": {
        "location": {
          "type": "string",
          "description": "Any of the following: latitude, longitude, state, zip code, city, etc."
        }
      },
      "required": [
      ]
    }
  }
]